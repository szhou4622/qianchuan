# v0.1.66 正式版 r24 本机修复

包含本轮r23的乘方指标读取、清除配置后的账户选择隔离、历史保护及提交授权复核修复。

- 真实联调确认：乘方配置接口列出的4项展现、点击类辅助指标会使报表返回40000下游依赖错误，但消耗、成交、ROI等核心指标单独请求成功。
- 核心指标和辅助指标分开读取；辅助指标出现已识别的下游错误时保留核心数据，辅助指标显示暂无数据，并在故障报告记录错误和请求编号。授权变化、核心报表失败或归属不明确仍阻止本轮入库。
- 本轮保持300秒采集周期，保留既有账户、激活、策略和历史数据。没有降低素材目录全量扫描频率。

本机可运行目录，不生成分发ZIP，不推送GitHub或服务器；r22保留作为回滚版本。
