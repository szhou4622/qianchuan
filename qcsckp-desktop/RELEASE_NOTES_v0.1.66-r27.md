# v0.1.66 正式版 r27 本机显示修复

- 包含r26的无当日报表记录按0展示；仅当天完整采集成功、10分钟内、明确未返回整条记录且已请求的指标适用。
- 补充显示缓存失效：即使没有新数据入库，过期或采集失败导致展示补零资格变化时，也会重新读取列表，撤销旧的展示零值。
- 接口失败、分页不完整、返回行缺字段、旧日期、过期记录和未请求字段仍显示“--”。
- 原始数据库及接口数值继续保留NULL，不改变规则输入、采集周期、历史数据或投放参数。

仅本机可运行目录，不生成分发ZIP，不发布GitHub或服务器。r26仅用于隔离验收，未切换生产；本机从r25直接切换r27。
