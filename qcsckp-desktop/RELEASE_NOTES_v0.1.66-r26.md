# v0.1.66 正式版 r26 本机显示修复

- 当天完整采集成功、数据未过期且报表未返回整条素材记录时，已请求的消耗、成交、ROI及流量字段在列表按0显示。
- 悬停说明保留展示来源；接口失败、分页不完整、返回行缺字段、旧日期和过期记录仍显示“--”。
- 数据库和后端数值保留原始空值，页面使用独立展示提示，避免零值展示影响规则、实际投放或历史流速；未请求字段及无法计算的预估ECPM不补0。
- 状态栏、汇总说明同步标明无当日记录按0显示。没有修改采集周期、账户、策略或投放参数。

仅本机可运行目录，不生成分发ZIP，不发布GitHub或服务器。
