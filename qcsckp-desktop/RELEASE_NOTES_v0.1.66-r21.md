# v0.1.66 正式版 r21

修复 r20 在激活码验证成功后无法进入主界面的问题。

- 原因：后台启动入口使用字符串动态导入，PyInstaller 遗漏 `utils.sqlite_prune_scheduler`。授权服务及激活码本身正常。
- 改为函数内显式导入，保留激活后才加载、启动后台的行为及原有启动/回滚顺序。
- 构建流程读取最终 EXE 的 PYZ 归档，检查全部 11 个后台模块及代码可解码性；任何缺失或损坏都阻止生成 ZIP。
- 授权通过但后台启动异常时，显示独立的“重试进入软件”，不再引导用户反复输入激活码或排查授权网络。
- 验收增加真正的授权后启动：同机现有许可密文复制至隔离空数据目录，正常在线验证后启动后台、加载主页并观察；不复制千川/飞书凭据或业务数据库，不激活新码、不执行真实投放。

保留 r20 的统一软件默认配置、采集恢复和依赖完整性修复。开发者本人和学员使用同一个包，各自保留私人配置。r20 原 ZIP 仅留作问题复现，暂勿分发。

本次仅本机修复、打包与验收，不推送 GitHub 或服务器。
