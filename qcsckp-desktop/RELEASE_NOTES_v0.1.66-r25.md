# v0.1.66 正式版 r25 本机修复

- 修复乘方配置与数据查询的统计范围不一致：配置和数据统一使用整体数据ALL_DATA，一次取得经营与流量指标，避免把整体数据指标用于不支持它们的乘方期间查询。
- 补回有展现/点击但消耗为0的素材报表行，不再用经营指标行作为流量数据的唯一匹配集合。
- 保留账户、抖音号、PC平台、素材类型和计划归属校验；整体数据增加检查同抖音号下其他全域计划，归属不明确时保留上轮数据。
- 区分“本轮暂无报表行”和“报表返回指标缺失”，前者不再显示为采集失败，也不会自动补0。
- 修复部分素材暂无报表导致整个消耗/成交/ROI曲线空白的问题；曲线显示已返回数据，真实字段缺失仍保留断点。
- 修复百分比小于1时的单位解析，避免将0.5%当作50%；新口径首次采集重建流速比较基准。
- 同日已取得非零消耗的素材异常消失时保留上次可信批次；故障报告保留完整请求范围和数据覆盖证据。

本机可运行版本，保留旧版和历史数据。采集周期仍为300秒。本轮不推送GitHub或服务器。
